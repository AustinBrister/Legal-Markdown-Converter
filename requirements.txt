# Legal Markdown Converter - Python Dependencies

# Web Framework
flask>=3.0.0
werkzeug>=3.0.0

# Native Desktop App (pywebview)
# Note: On Windows, uses Edge WebView2 (built into Windows 10/11)
# We exclude pythonnet as it has build issues and isn't needed for EdgeChromium
pywebview>=5.0.0

# Markdown Processing
markitdown[pdf]==0.1.2
markdown-it-py>=3.0.0

# PDF Processing
PyMuPDF>=1.23.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Email Processing (EML/MSG)
extract-msg>=0.48.0

# =============================================================================
# External dependencies (installed automatically by launch scripts)
# =============================================================================
# The launch scripts (launch.command on macOS, launch.bat on Windows) will
# automatically install the following if they're missing:
#
# - Python 3
# - Pandoc (document format conversion)
# - Tesseract OCR (for scanned PDF text extraction)
#
# macOS: Installed via Homebrew (brew install)
# Windows: Installed via winget (Windows Package Manager)
#
# =============================================================================
# Manual installation (if automatic installation fails)
# =============================================================================
#
# PYTHON 3.10+
#   macOS:   brew install python
#   Windows: https://www.python.org/downloads/
#            IMPORTANT: Check "Add Python to PATH" during installation
#
# PANDOC (document format conversion)
#   macOS:   brew install pandoc
#   Windows: https://pandoc.org/installing.html
#            (The installer adds to PATH automatically)
#
# TESSERACT OCR (for scanned PDF text extraction)
#   macOS:   brew install tesseract
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#            IMPORTANT: Check "Add to PATH" during installation
#            If missed, manually add to PATH: C:\Program Files\Tesseract-OCR
# =============================================================================
